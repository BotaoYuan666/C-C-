　　有一文本文件，格式参见附件1.txt。文件由多条记录组成，每条记录为一行，都是以<HEAD>开始，以</HEAD>结束；每条记录中又包含若干项，称之为域，每个域都有域名，
并有具体的内容。现需要统计该文件中的如下数据：
1、总的记录数； 
2、文件中出现的域名总数(HEAD除外)；
3、最长记录所占的字节数；
4、在所有记录中，找出相同域名对应的域内容的最大长度，并记录其域名、位于
第几条记录、具体的域内容。
使用命令行的格式实现上述要求，并把上述所有的统计数据均要输出到一个
文本文件中保存，可参照如下格式：
记录总数为          :
出现域的总数为      :
最长纪录所占字节数为:

域名为      :
最大长度为  :
所处记录号为:
域内容为    :